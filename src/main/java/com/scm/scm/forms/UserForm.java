package com.scm.scm.forms;

import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Size;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder

public class UserForm {
	/*
	 * @NotBlank(message = "Name is required")
	 * 
	 * @Size(min = 3, message = "Min 3 Characters is required") private String name;
	 * 
	 * @NotBlank(message = "Username is required")
	 * 
	 * @Size(min = 4, message = "Min 4 Characters with unique username is required")
	 * private String username;
	 * 
	 * @Email(message = "Invalid Email Address")
	 * 
	 * @NotBlank(message = "Email is required") private String email;
	 * 
	 * @NotBlank(message = "Password is required")
	 * 
	 * @Size(min = 6, message = "Min 6 Characters is required") private String
	 * password;
	 * 
	 * @NotBlank(message = "Role is required") private String role;
	 * 
	 * public void setUid(String string) { // TODO Auto-generated method stub
	 * 
	 * }
	 */

	

}
